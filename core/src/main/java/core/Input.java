package core;

import core.controls.Keyboard;
import core.controls.Mouse;

public class Input {

  public static boolean getKeyDown(int code) {
    var keyboard = Keyboard.get();
    return Keyboard.isKeyPressed(name)
  }
}
